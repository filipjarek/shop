<header class="w-full container mx-auto">
	<div class="flex flex-col items-center py-8">
		<a class="font-bold uppercase text-gray-700 text-5xl tracking-wide" href="{{ path('app_home') }}">
			SHOP
		</a>
	<div class="flex flex-grid items-end justify-end text-gray-600">
		<a class="py-2 px-1 text-lg text-gray-400 hover:text-black" href="{{ path('app_login') }}">
			Login</a>
		<a class="py-2 px-1 text-lg text-gray-400 hover:text-black" href="{{ path('app_register') }}">
		|	Register</a>
		<a class="py-2 px-1 text-lg text-gray-400 hover:text-black" href="{{ path('app_cart') }}">
		|	<i class="fa-solid fa-basket-shopping"></i></a>
	</div>
</header>
