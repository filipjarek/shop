<nav class="w-full bg-white">
	<div class="w-full container mx-auto flex flex-col sm:flex-row items-center justify-center text-sm font-medium uppercase py-1">

		{% for category in categories %}

			<a href="{{ path('app_category', {'slug': category.slug}) }}" class="text-gray-600 py-1 px-2 mx-2"> 
				<p class="text-lg font-medium hover:text-black">{{ category.name }}</p>
			</a>

		{% endfor %}
		
	</div>
</nav>

